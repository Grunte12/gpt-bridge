# Normalize text files to LF in the repo; Git still checks them out with the
# platform's native line ending when core.autocrlf is set.
* text=auto eol=lf

# Explicit binary types (Git also auto-detects these, but be explicit).
*.png binary
*.ico binary
*.lock -text
